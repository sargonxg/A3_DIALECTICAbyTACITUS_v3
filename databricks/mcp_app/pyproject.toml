[project]
name = "tacitus-capsule-factory-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "databricks-sdk",
  "databricks-sql-connector",
  "mcp",
  "pandas",
]

[project.scripts]
tacitus-mcp-server = "server.main:main"
